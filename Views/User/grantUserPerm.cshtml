
<div class="topLinks">
    <div class="goBackLinks">
        <a href="/grantAccessPage">Go Back</a>
    </div>
    <div class="dashLogLinks">
        <a href="/dashboard">Dashboard</a>
        <a href="/logout">Logout</a>
    </div>
</div>

<h2>Select permission level for this user:</h2>

<h4>@ViewBag.user.FirstName @ViewBag.user.LastName</h4>
<h4>Email: @ViewBag.user.Email</h4>
<h4>Organization: @ViewBag.user.Organization</h4>
<div id="permText">
    <h4>Current Permission Level:
        @{if(ViewBag.user.Permission == 1){
            <h4 class="permAlert">View Only</h4>
        }
        @if(ViewBag.user.Permission == 0){
            <h4 class="permAlert">No Access</h4>
        }
        @if(ViewBag.user.Permission == 5){
            <h4 class="permAlert">Write Access</h4>
        }
        @if(ViewBag.user.Permission == 9){
            <h4 class="permAlert">Admin</h4>
        }}
    </h4>
</div>

<form action="givePerm" method="post" id="permForm">
    <input type="hidden" name="id" value="@ViewBag.user.id">
    Edit Permission Level: <select name="perm">
        <option value=1>View Only</option>
        <option value=5>Write Access</option>
        <option value=9>Admin</option>
    </select><br>
    <input type="submit" value="Grant Permission">
</form>
<br>
<h2>Or...</h2>
<h3><a href="deleteUserPage/@ViewBag.user.id" class="deleteLink">Delete this user</a></h3>
<br>
<br><br><br>
<p>View Only: User can view child and assessment information, but cannot make any changes.</p>
<p>Write Access: User can add children, assessment scores, and dates, but cannot change existing info or delete anything.</p>
<p>Admin: User can make any changes, including adding, deleting, and updating; user can also grant access to other users.</p>
